# Remediation Log

Chronological. Newest at bottom. One line per change: `YYYY-MM-DD — item — what happened`.

2026-06-19 — setup — Read Comprehensive analysis.md + report-safe-remediation-plan.md. Created remediation/ tracking folder (PROGRESS.md checklist of all 38 items, this log). No code changed yet; Phase 0 is the next work.
2026-06-19 — baseline — Full suite green before changes: 13 files / 114 tests. Local Mongo+Redis up.
2026-06-19 — C1 — POST /api/reports now requires `authenticate`; identity (user_id/reported_for_user_id/reporter) derived from the principal, never trusted from body; gov token trusted for overrides. Web+mobile submitReport now attach the Bearer token + refresh-retry on 401. Tests: 401-on-unauth + identity-derivation added to reportsProxy.
2026-06-19 — C3 — MockMeshTransport.sendToPeer returns false (no real transport → never claims delivery); offline reports stay `pending` and re-flush on reconnect. outboxDb caps table at 200 rows, evicting only DELIVERED rows. Test: failed relay stays pending → delivered on reconnect.
2026-06-19 — C4 — New lib/leaderLock.js (Redis SET NX PX). triggerEngine/missingPerson/retention/stats schedulers now run their tick only if leader; single-instance (no Redis) always leads. Core fns untouched so unit tests unaffected.
2026-06-19 — C2 — All four geo bounding-box queries (rescue, shelters, findDevicesInRadius, findAffectedUsersInRadius) bounded with .sort(indexed).limit(GEO_SCAN_CAP=5000) before toArray(). Suite green: 117 tests.
2026-06-19 — Phase 1 (H1/B2/B4) — Mobile+web outbox now treat ONLY 400/422 as permanent; 401/403/408/429/5xx stay queued (isPermanentRejection helper). Report ingest moved to its own user-keyed limiter (max 600/min) and excluded from the general /api IP limiter. B4 idempotency proven by new "relay never overwrites status" test. +H1 sync regressions (429 pending, 422 dropped). 120 tests.
2026-06-19 — Phase 2 (H2/H3/H4/B21/H5/B22) — trust proxy default 0 + prod warn; CSP header (overridable) + HSTS default-on in prod; refresh-token reuse detection (prev_refresh_token_hash tripwire → token_reuse, family nuked) + index; DELETE /api/devices/:token now owner-or-gov (401/403); GET /stats excludeWeb defaults true; ci.yml fixed label + eslint + npm audit + gitleaks + CodeQL jobs. Tests: tokenRefresh.test.js, devicesAuth.test.js, H5 stats. 127 tests / 15 files.
2026-06-19 — Phase 3 (M1/M4/B20/M2/B19) — PDPO erasure now scrubs user-doc PII in place + tombstones FIRST (phone sentinel, $unset personal_id), then cascades + deletes; retention sweep finalizes crash-stranded tombstones (finalizePendingErasures). Partial-unique (type,active) disaster index + 11000-catch returns existing; POST /api/disasters/:id/deactivate (gov) + disaster_deactivated socket event. OTP store moved to Redis (async verifyOtp) with in-memory fallback. New /api/missing-persons CRUD (gov/volunteer) wiring the dead missing_alert event; mounted in index.js. Removed dead report_update event (server side). Tests: phase3.test.js, M1 sweep, otp awaits. 132 tests / 16 files.
2026-06-19 — Phase 4 (H7/M5/M7/M8/M9) — Name search anchored to prefix (^q) on indexed name field. Wildcard ($**) indexes dropped; every find().sort() field now has a targeted index (added started_at, name, created_at, updated_at across collections) — aggregation $sort on computed keys never used the wildcard. Admin users list gained optional ?after cursor (next_cursor) keeping offset default. Seed default 10000→100 + gated behind SEED_DATA (off in prod). Redis SPOF: general /api limiter fail-closed on Redis error, ingest fail-open; added /api/live, /api/ready (DB+Redis ping), /api/metrics (req count/error rate/latency/sockets); metrics counters in requestLogger. Tests: H7 prefix. 133 tests.
2026-06-19 — Phase 5 (L1-L8/M6) — CORS refuses "*" in prod; lib/mongoMap.js shared helpers adopted across 6 files; dead rescue_requests/team_assignments collections removed+dropped; report_update event removed (server+web); central lib/errorHandler.js (reqId, no stack leak); generateData/fillDatabase moved to server/scripts + .npmignore; admin IP allowlist (M6). Docs: GOV_TOKEN default corrected, compose Postgres→Cosmos + prod toggles (OTP/SEED/proxy), new DEPLOYMENT.md env table. L7 (envelope rewrite) deferred — cosmetic, breaks every client; error envelope already standardized in L6.
2026-06-19 — VERIFY — All four CI gates pass locally: eslint(server/src) clean, 133 tests / 16 files, mobile `tsc --noEmit` clean, `npm run build:web` clean. Remediation plan complete (37/38 done, L7 deferred with rationale).
2026-06-20 — L7/H3/M7 (follow-up: completed the 3 deferred/partial items) —
  L7: every list/resource success response → { ok, data, meta }; auth-token + /live//ready//metrics stay flat. Web api.js reshape() re-exposes data under each view's legacy key (views untouched); mobile reads body.data. Tests updated to data/meta.
  H3: refresh token now an httpOnly; Secure; SameSite=Strict cookie (Path=/api/users/token/refresh) set on register/login/refresh; refresh reads cookie-first then body (mobile). Web access token in memory only; localStorage tokens purged; initAuth() rehydrates from cookie on load.
  M7: admin links list gained ?after cursor + next_cursor (last find().skip removed); reports/devices stay offset (computed-urgency sort). Cursor test added on /users.
  Verify: 135 tests / 16 files, eslint(server/src) clean, mobile tsc clean, web build clean. ALL 38 items now complete.
2026-06-22 — REVIEW (full re-audit for production sign-off) — Re-verified all 38 items against the code: every C/H/M/L/B fix is genuinely present and correct. Baseline green: 143 tests / 18 files, mobile tsc clean, web build clean, eslint(server/src) 0 errors (10 intentional omit-destructuring warnings). Two REAL bugs found + fixed, one doc-rot:
  • H1 (regression hole) — web/src/views/ReportView.vue inline submit handler still dropped ANY 4xx via markSent(id); a 429/408 on a logged-in web submit silently lost the report (the H1 fix had only been applied to useOutbox.retryAll, not this second path). Now drops on 400/422 only, matching useOutbox + mobile syncService. Verified no other 4xx-permanent-drop paths remain.
  • M1 (dead recovery path) — finalizePendingErasures() crash-recovery sweep only ran when RETENTION_DAYS>0 (off by default), so a crash mid-erasure permanently stranded a PII-free tombstone + its un-cascaded device tokens. retentionService.startRetention now runs a leader-gated boot sweep regardless of RETENTION_DAYS.
  • Doc rot — root vitest.config.mjs comment still said "Postgres"; corrected to MongoDB.
  Uncommitted WIP reviewed (utils/location.ts, utils/severity.ts, acknowledgeAllInZone, notification payload): clean refactors with tests; resolveLocation also fixes a latent GPS-hang that could trap a safety report on the spinner. Deliverables added: QA_TEST_PLAN.md, UAT_TEST_PLAN.md, remediation/POST_REMEDIATION_REVIEW.md.
