node_modules
.expo/
.expo-shared/
dist/
web-build/
*.log
.env
.env.*
# …but keep the committed templates (real secrets stay ignored above).
!.env.example
!.env.prod.example

# stale local database artifacts
*.db
*.db-shm
*.db-wal

# Mobile push credentials — secrets/project files, never commit.
# google-services.json: drop it in mobile/ before an Android build (or store as
# an EAS secret file). AuthKey_*.p8 / GoogleService-Info.plist likewise.
mobile/google-services.json
mobile/GoogleService-Info.plist
**/AuthKey_*.p8
*.p8
.agents/
